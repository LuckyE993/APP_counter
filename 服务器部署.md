# Beancount记账应用 - 服务器部署指南

本文档详细说明如何将本项目部署到Linux服务器（推荐Ubuntu 20.04/22.04或CentOS 7/8）

## 📋 目录

- [环境要求](#环境要求)
- [第一步：服务器准备](#第一步服务器准备)
- [第二步：安装依赖环境](#第二步安装依赖环境)
- [第三步：部署后端服务](#第三步部署后端服务)
- [第四步：部署前端应用](#第四步部署前端应用)
- [第五步：配置Nginx反向代理](#第五步配置nginx反向代理)
- [第六步：配置进程守护](#第六步配置进程守护)
- [第七步：SSL证书配置（可选）](#第七步ssl证书配置可选)
- [认证配置](#认证配置)
- [维护与更新](#维护与更新)
- [故障排查](#故障排查)

---

## 环境要求

### 服务器配置
- **CPU**: 1核心或以上
- **内存**: 1GB或以上
- **硬盘**: 10GB或以上
- **操作系统**: Ubuntu 20.04/22.04 或 CentOS 7/8

### 软件版本
- Python 3.10+
- Node.js 18+
- Nginx 1.18+
- Git

---

## 第一步：服务器准备

### 1.1 连接服务器

```bash
ssh root@your_server_ip
```

### 1.2 更新系统

**Ubuntu/Debian:**
```bash
apt update
apt upgrade -y
```

**CentOS/RHEL:**
```bash
yum update -y
```

### 1.3 创建部署用户（推荐）

```bash
# 创建用户
useradd -m -s /bin/bash appuser

# 设置密码
passwd appuser

# 添加sudo权限
usermod -aG sudo appuser

# 切换到新用户
su - appuser
```

### 1.4 创建项目目录

```bash
mkdir -p ~/apps/beancount
cd ~/apps/beancount
```

---

## 第二步：安装依赖环境

### 2.1 安装Python 3.10+

**Ubuntu/Debian:**
```bash
sudo apt install -y python3.10 python3.10-venv python3-pip
```

**CentOS/RHEL:**
```bash
sudo yum install -y python3 python3-pip python3-devel
```

验证安装：
```bash
python3 --version
```

### 2.2 安装Node.js 18+

**使用NodeSource仓库（推荐）:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 或者CentOS
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
```

验证安装：
```bash
node --version
npm --version
```

### 2.3 安装Nginx

**Ubuntu/Debian:**
```bash
sudo apt install -y nginx
```

**CentOS/RHEL:**
```bash
sudo yum install -y nginx
```

启动Nginx：
```bash
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 2.4 安装Git

```bash
sudo apt install -y git  # Ubuntu
# 或
sudo yum install -y git  # CentOS
```

---

## 第三步：部署后端服务

### 3.1 上传代码

**方法一：使用Git（推荐）**
```bash
cd ~/apps/beancount
git clone <your_repository_url> .
```

**方法二：使用SCP上传**
```bash
# 在本地电脑执行
scp -r F:\APP_counter\backend appuser@your_server_ip:~/apps/beancount/
```

### 3.2 创建Python虚拟环境

```bash
cd ~/apps/beancount/backend
python3 -m venv venv
source venv/bin/activate
```

### 3.3 安装Python依赖

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 3.4 配置环境变量

```bash
cd ~/apps/beancount/backend
nano .env
```

编辑`.env`文件（根据实际情况修改）：
```env
# VLM Provider Configuration
VLM_PROVIDER=openai
VLM_API_KEY=your_api_key_here

# OpenAI Configuration
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=https://api.openai.com/v1

# Claude Configuration
CLAUDE_MODEL=claude-opus-4-5-20251101

# Beancount Configuration
BEANCOUNT_MAIN_PATH=/home/appuser/apps/beancount/backend/data/main.beancount
BEANCOUNT_TRANSACTION_PATH=/home/appuser/apps/beancount/backend/data/2026/transactions.beancount
```

### 3.5 创建数据目录和账本文件

```bash
mkdir -p ~/apps/beancount/backend/data/2026
```

确保以下文件存在：
- `data/main.beancount`
- `data/accounts.beancount`
- `data/commodities.beancount`
- `data/2026/2026.beancount`
- `data/2026/initial.beancount`
- `data/2026/transactions.beancount`

### 3.6 测试后端服务

```bash
cd ~/apps/beancount/backend
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

访问 `http://your_server_ip:8000/docs` 查看API文档

按 `Ctrl+C` 停止测试服务

---

## 第四步：部署前端应用

### 4.1 上传前端代码

如果使用Git已经包含前端代码，跳过此步。否则：
```bash
# 在本地电脑执行
scp -r F:\APP_counter\frontend appuser@your_server_ip:~/apps/beancount/
```

### 4.2 安装依赖并构建

```bash
cd ~/apps/beancount/frontend
npm install
```

### 4.3 配置API地址

编辑 `frontend/vite.config.js`，添加代理配置：
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  }
})
```

### 4.4 构建生产版本

```bash
cd ~/apps/beancount/frontend
npm run build
```

构建完成后，生成的文件在 `frontend/dist/` 目录

---

## 第五步：配置Nginx反向代理

### 5.1 创建Nginx配置文件

```bash
sudo nano /etc/nginx/sites-available/beancount
```

添加以下配置：
```nginx
server {
    listen 80;
    server_name 188.239.19.114;  # 替换为你的域名或服务器IP

    # 前端静态文件
    location / {
        root /home/qixuan/APP_counter/frontend/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # 后端API代理
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Fava代理（端口5000）
    location /fava {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        rewrite ^/fava(.*)$ $1 break;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /home/qixuan/APP_counter/frontend/dist/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

### 5.2 启用配置

**Ubuntu/Debian:**
```bash
sudo ln -s /etc/nginx/sites-available/beancount /etc/nginx/sites-enabled/
```

**CentOS/RHEL:**
```bash
sudo cp /etc/nginx/sites-available/beancount /etc/nginx/conf.d/beancount.conf
```

### 5.3 测试并重载Nginx

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## 第六步：配置进程守护

使用Systemd管理后端服务，确保服务自动启动和重启

### 6.1 创建后端服务文件

```bash
sudo nano /etc/systemd/system/beancount-backend.service
```

添加以下内容：
```ini
[Unit]
Description=Beancount Backend API Service
After=network.target

[Service]
Type=simple
User=appuser
Group=appuser
WorkingDirectory=/home/appuser/apps/beancount/backend
Environment="PATH=/home/appuser/apps/beancount/backend/venv/bin"
ExecStart=/home/appuser/apps/beancount/backend/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000 --workers 2
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 6.2 启动并启用服务

```bash
sudo systemctl daemon-reload
sudo systemctl start beancount-backend
sudo systemctl enable beancount-backend
```

### 6.3 检查服务状态

```bash
sudo systemctl status beancount-backend
```

### 6.4 查看日志

```bash
# 查看实时日志
sudo journalctl -u beancount-backend -f

# 查看最近100行
sudo journalctl -u beancount-backend -n 100
```

---

## 认证配置

本应用已集成JWT认证机制，提供安全的用户登录功能。

### 认证特性
- JWT token认证，30天有效期
- 自动token刷新和持久化存储
- 401错误自动跳转登录页面
- 所有API端点都需要认证（除登录接口）

### 配置认证信息

编辑后端配置文件：
```bash
cd /opt/beancount-app/backend
nano .env
```

设置认证相关配置：
```env
# JWT密钥 - 生产环境必须更换为复杂的随机字符串
SECRET_KEY=your-very-secure-secret-key-change-in-production

# 管理员账号 - 生产环境请修改默认密码
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password

# Token过期时间（分钟，默认30天）
ACCESS_TOKEN_EXPIRE_MINUTES=43200
```

### 生成安全的SECRET_KEY

```bash
# 生成随机密钥
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 首次登录

部署完成后，访问应用会自动跳转到登录页面：
- 默认用户名：`admin`
- 默认密码：`admin123`（请在生产环境中修改）

登录成功后，token会自动保存在浏览器本地存储中，30天内无需重复登录。

---

## 第七步：SSL证书配置（可选）

使用Let's Encrypt免费SSL证书

### 7.1 安装Certbot

**Ubuntu/Debian:**
```bash
sudo apt install -y certbot python3-certbot-nginx
```

**CentOS/RHEL:**
```bash
sudo yum install -y certbot python3-certbot-nginx
```

### 7.2 获取证书

```bash
sudo certbot --nginx -d your_domain.com
```

按提示输入邮箱并同意服务条款

### 7.3 自动续期

Certbot会自动配置续期，验证：
```bash
sudo certbot renew --dry-run
```

---

## 维护与更新

### 更新应用代码

```bash
cd ~/apps/beancount

# 拉取最新代码
git pull

# 更新后端
cd backend
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart beancount-backend

# 更新前端
cd ../frontend
npm install
npm run build
sudo systemctl reload nginx
```

### 查看日志

```bash
# 后端日志
sudo journalctl -u beancount-backend -f

# Nginx访问日志
sudo tail -f /var/log/nginx/access.log

# Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 备份数据

```bash
# 备份账本数据
tar -czf ~/backup/beancount-data-$(date +%Y%m%d).tar.gz \
  ~/apps/beancount/backend/data/

# 定期备份（添加到crontab）
crontab -e
# 添加：每天凌晨2点备份
0 2 * * * tar -czf ~/backup/beancount-data-$(date +\%Y\%m\%d).tar.gz ~/apps/beancount/backend/data/
```

---

## 故障排查

### 问题1：后端服务无法启动

```bash
# 检查服务状态
sudo systemctl status beancount-backend

# 查看详细日志
sudo journalctl -u beancount-backend -n 100

# 常见原因：
# 1. 端口被占用
sudo lsof -i :8000

# 2. 环境变量配置错误
cat backend/.env

# 3. Python依赖问题
cd ~/apps/beancount/backend
source venv/bin/activate
pip list
```

### 问题2：前端无法访问后端API

```bash
# 检查Nginx配置
sudo nginx -t

# 查看Nginx错误日志
sudo tail -f /var/log/nginx/error.log

# 测试后端API
curl http://localhost:8000/health
```

### 问题3：Fava无法访问

```bash
# 检查Fava进程
ps aux | grep fava

# 手动启动测试
cd ~/apps/beancount/backend
source venv/bin/activate
fava data/main.beancount -p 5000
```

### 问题4：防火墙阻止访问

```bash
# Ubuntu UFW
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw status

# CentOS FirewallD
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 问题5：磁盘空间不足

```bash
# 查看磁盘使用
df -h

# 查看大文件
du -h --max-depth=1 /home/appuser | sort -hr

# 清理日志
sudo journalctl --vacuum-time=7d
```

---

## 性能优化建议

### 1. 使用Gunicorn代替Uvicorn

安装Gunicorn：
```bash
pip install gunicorn
```

修改systemd服务文件：
```ini
ExecStart=/home/appuser/apps/beancount/backend/venv/bin/gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 127.0.0.1:8000
```

### 2. 启用Nginx Gzip压缩

在Nginx配置中添加：
```nginx
gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
gzip_vary on;
```

### 3. 配置防火墙限流

```bash
# 使用fail2ban防止暴力攻击
sudo apt install fail2ban
```

---

## 安全建议

1. **定期更新系统和软件包**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

2. **使用强密码和SSH密钥**
   ```bash
   ssh-keygen -t rsa -b 4096
   ```

3. **禁用root登录**
   ```bash
   sudo nano /etc/ssh/sshd_config
   # 设置 PermitRootLogin no
   sudo systemctl restart sshd
   ```

4. **定期备份重要数据**

5. **监控服务器资源**
   ```bash
   sudo apt install htop
   htop
   ```

---

## 总结

完成以上步骤后，你的Beancount记账应用应该已经成功部署到服务器并可以通过域名或IP访问。

访问地址：
- 前端应用：`http://your_domain.com`
- API文档：`http://your_domain.com/api/docs`
- Fava界面：通过前端的"余额"页面访问

如有问题，请参考故障排查部分或查看相关日志文件。
